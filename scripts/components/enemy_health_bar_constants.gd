## Константы для соседнего скрипта логики.
## Значения собраны здесь, чтобы их можно было просматривать и настраивать отдельно.
class_name EnemyHealthBarConstants
extends RefCounted

const BAR_WIDTH: float = 1.0

const BAR_HEIGHT: float = 0.13

const BG_COLOR: Color = Color(0.08, 0.05, 0.05, 0.85)

const FILL_COLOR: Color = Color(0.85, 0.12, 0.12, 1.0)
